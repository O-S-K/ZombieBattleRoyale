using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ZombieBattleRoyale
{
    public enum WeaponCategory { Ranged,  Melee }
    public enum FireModeType { Single, Auto, Burst }
    public enum ProjectileType { Bullet, Rocket, Mortar, Laser }
    public enum ReloadType {  Cooldown }
}
